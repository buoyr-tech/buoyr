import 'package:flutter/material.dart';

class MakeWavesSpacer extends StatelessWidget {
  const MakeWavesSpacer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: 50.0);
  }
}
